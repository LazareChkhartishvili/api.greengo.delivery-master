[build]
builder = "nixpacks"

[deploy]
startCommand = "php artisan migrate --force && php artisan db:seed --class=FullProductSeeder --force && echo 'Seeding completed' && php -S 0.0.0.0:$PORT -t public/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[variables]
PORT = "8080"
